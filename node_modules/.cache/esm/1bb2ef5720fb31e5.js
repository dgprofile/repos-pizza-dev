let JWT_SECRET,jwt;_20f‍.x([["default",()=>_20f‍.o]]);_20f‍.w("../config",[["JWT_SECRET",["JWT_SECRET"],function(v){JWT_SECRET=v}]]);_20f‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);


class JwtService {
    static sign(payload, expiry = '1y', secret = JWT_SECRET) {
        return jwt.sign(payload, secret, { expiresIn: expiry });
    }

    static verify(token, secret = JWT_SECRET) {
        return jwt.verify(token, secret);
    }
}

_20f‍.d(JwtService);